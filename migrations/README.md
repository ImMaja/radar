# Migrations Radar

Alembic est l'unique autorité de modification du schéma PostgreSQL. Les
migrations lisent `RADAR_DATABASE_URL` par la configuration typée de
l'application et ne contactent aucun service externe.
